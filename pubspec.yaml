name: test_debstech_riverpod2_todo_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  firebase_core:
  sqflite:
  google_fonts:
  flutter_screenutil: ^5.9.0
  flutter_vector_icons:
  country_picker:
  date_time_picker:
  dynamic_color:
  smooth_page_indicator:
  hooks_riverpod: ^2.4.3
  flutter_datetime_picker_plus:
  riverpod_annotation:
  pinput:
  firebase_auth:
  flutter_local_notifications:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

  build_runner:
  riverpod_generator:

flutter:

  uses-material-design: true

  assets:
    - assets/images/
